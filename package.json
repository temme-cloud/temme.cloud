{
  "name": "temme-cloud-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:check": "test -f static/js/delivery-check.js || (echo '❌ Run npm run build first!' && exit 1)",
    "hugo": "npm run build:check && hugo",
    "hugo:server": "npm run build:check && hugo server",
    "all": "npm run build && hugo --minify",
    "preview": "vite preview"
  },
  "dependencies": {
    "solid-js": "^1.8.0"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.10.0"
  }
}
